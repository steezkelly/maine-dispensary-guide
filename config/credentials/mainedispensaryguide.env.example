SMTP_EMAIL=steve@mainedispensaryguide.com
SMTP_PASSWORD=<replace-with-purelymail-app-password>

# Optional: explicit host/port values for local overrides.
# Defaults in scripts/send-email.cjs are:
# SMTP_HOST=smtp.purelymail.com
# SMTP_PORT=465
# SMTP_SECURE=true

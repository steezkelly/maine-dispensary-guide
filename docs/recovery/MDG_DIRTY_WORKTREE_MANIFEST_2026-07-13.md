# MDG dirty-worktree recovery manifest — 2026-07-13

## Snapshot and safety boundary

- Canonical remote at inventory time: `origin/main` = `429324747542b32d6dd0aa50e6d7817b26a31727`.
- This is an inventory, not a cleanup authorization. No path listed here was deleted, restored, bulk-added, or moved.
- The primary checkout remains an observational recovery source only: `/home/steve/projects/maine-dispensary-guide`.
- Generated by direct NUL-delimited `git status --porcelain=v1 -z` inspection plus file-type and content-diff spot checks.

## Primary checkout: 994 entries

| Disposition | Count | Paths / evidence | Next action |
| --- | ---: | --- | --- |
| Unknown — preserve | 924 | Tracked binary changes under `apps/maine-cannabis/public/images/heroes/`; sampled JPEG/WebP assets differ from `HEAD`, retain 1280×720 dimensions, and were written in a tight generation window. | Keep untouched until image-pipeline provenance and visual QA identify the producing branch/run. Do not run `git restore`, `git clean`, or a bulk commit. |
| Unknown — preserve | 60 | Untracked `apps/maine-cannabis/public/images/heroes/*.{bak,*.v1.bak,*.v2.bak,*.v3.bak}` backups. | Keep as recovery material while the hero-asset mutation set is investigated. |
| Source asset candidate — preserve | 6 | New `maine-cannabis-cultivation-license-2026-text` AVIF/JPEG/WebP full + 640w variants. | Compare to page references and image-pipeline manifest before staging; no automatic integration. |
| Agent-local state | 1 | `.hermes/`. | The reliability branch ignores this path; leave in place locally. |
| Nested-worktree container | 1 | `.worktrees/`. | Keep visible to Git; inventory its registered children through `workflow:status`, not by ignoring or deleting it. |
| Session-state documents — preserve | 2 | `docs/SESSION_PASSDOWN_OUT_2026-07-12.md`, `docs/SESSION_PASSDOWN_OUT_2026-07-13.md`. | Read and reconcile into a later state-of-record update; do not bulk-stage with image assets. |

## Other dirty worktrees

| Worktree | Dirty paths | Classification | Disposition |
| --- | --- | --- | --- |
| `/home/steve/projects/maine-dispensary-guide/.worktrees/t_d7138d70` | `download/compliance-self-assessment.astro`, `download/metrc-reconciliation-checklist.astro` | Needs owner/semantic decision | The committed mailto gate exists on `origin/main` (`b1fd5971`). The remaining 138-addition/5-deletion edits per page are alternate form layout/copy/theme variants on the same surfaces. Preserve in place; do not auto-cherry-pick or overwrite the canonical implementation. |
| `/home/steve/projects/maine-dispensary-guide-faq-jsonld` | `.agents/`, `.hermes/` | Agent-local state | Preserve locally; no source path is dirty. |
| `/home/steve/projects/maine-dispensary-guide-readability-audit` | `.agents/readability-audit-2026-07-13.md`, `packages/seo/`, `packages/ui/node_modules` | Audit handoff + generated/bootstrap state | Preserve the audit document. `packages/seo/` is an empty 4 KiB directory and `packages/ui/node_modules` is a symlink; neither is staged or removed automatically. The audit describes cross-theme contrast work requiring its own reviewed implementation branch. |

## Recovery controls

1. All future recovery work uses a new, clean worktree based on freshly fetched `origin/main`.
2. Any source-only recovery must name its originating worktree, touched paths, focused verification, and exact commit in a follow-up entry.
3. Image artifacts require an auditable producer/variant manifest plus visual validation before they can move from `Unknown — preserve` to an integration candidate.
4. The two lead-form variants are intentionally held for a semantic decision because automatically choosing either layout would override an already-integrated canonical lead-capture surface.

## Related evidence

- Live machine-readable worktree inventory: `node scripts/git/mdg-worktree-status.cjs --json`.
- Branch inventory at this snapshot: `/tmp/mdg-remote-branch-triage-2026-07-13.json` (transient analysis output; its final reconciled decisions belong in a committed branch-triage manifest).
